# T3TR1S  *by kakoritz*

A hand-engineered NES-style Tetris clone written from scratch in Python and Pygame.  
No sprite sheets. No audio files. No game engine. Every pixel, sound, and note is
synthesised at runtime from first principles.

---

## What this actually is

This is not a tutorial project assembled from scaffolding.  
Every subsystem was designed, implemented, and tuned by hand:

**Procedural audio synthesis**  
All sound effects and music are generated by writing raw PCM samples into WAV buffers
using NumPy. Sound effects use band-limited sawtooth and square-wave oscillators with
per-voice decay envelopes tuned per piece type. The music engine produces ten
independent "tiers" of a layered arrangement and streams them as looped WAVs through
Pygame's mixer. The track adapts in real time — it monitors board state every frame
and switches between tiers and danger mode without any audio files on disk.

**Custom game loop and state machine**  
The main loop owns its own fixed-timestep gravity tick, DAS (Delayed Auto-Shift)
repeat logic with charge/repeat phases, line-clear flash animation phasing, particle
burst emission, screen-shake, and hard-drop impact flash — all coordinated through
explicit state variables. There is no scene graph, no entity component system, no
coroutines. Every frame is accounted for.

**Per-block physics animation**  
The GAME OVER sequence runs each pixel block of the "GAME OVER" text as an independent
physics body: individual gravity, bounce damping coefficient, and a randomised release
delay. Letters fall in chaos order rather than in sync. Impact sounds fire the moment
each complete letter sticks.

**Adaptive music system**  
The game monitors board state every frame. If any block occupies the top-10 rows a
tension track kicks in, a pulsing red warning line appears at the row-10 boundary,
and the score multiplier for clearing rows in the danger zone doubles. When the board
clears, the full music sequence resumes from the start. A perfect clear (board
completely empty after a line clear) triggers a separate rainbow flash event and a
large score bonus.

**Resolution scaling**  
The game renders to a fixed 460×600 logical surface, then scales the output to the
user's chosen window size using bilinear interpolation. The scale preference is
persisted across sessions in `config.json`. Ghost-piece opacity is also persisted.

---

## Features

- All 7 classic tetrominoes with NES-palette colours
- **7-bag randomiser** — every piece type appears once per bag before repeating; no droughts
- **SRS wall kicks** — full Tetris Guideline kick tables for JLSZT and I pieces (5 tests each)
- **Lock delay** — 500 ms grace period after landing; move or rotate resets the clock (15-reset cap)
- **Hold piece** — press `C` to stash the current piece; swap back at any time (once per piece)
- **T-spin detection** — 3-corner rule; full T-spin and mini both detected and scored separately
- **Back-to-back bonus** — consecutive Tetrises or T-spins earn 1.5× score on the difficult clear
- **Combo counter** — consecutive line clears stack a 50 × combo × (level + 1) bonus per clear
- **20G gravity** — at level 20, pieces drop to the floor instantly on each gravity tick
- CW and CCW rotation
- DAS (Delayed Auto-Shift) — tap to move, hold to auto-repeat
- Ghost piece with adjustable opacity (0 % invisible → 100 % solid, default 15 %)
- Line-clear flash — escalates from white (single) to gold (Tetris)
- Particle burst on every line clear
- Screen shake on a 4-line Tetris clear
- Hard-drop impact flash
- **10-tier layered chiptune soundtrack** — procedurally synthesised, no audio files
- **Adaptive music** — switches tiers based on board state in real time
- **Danger zone** — pulsing red warning line at row 10; music drops to tension track
- **Danger bonus** — rows cleared above the red line score 2× with floating ×2 labels
- **WOW / perfect clear** — clears that leave the board completely empty trigger a
  full-board rainbow flash, a large centred popup, score bonus, and max particles
- Per-piece spawn tones + move / rotate / lock / hard-drop SFX
- Sidebar feedback popups: *Nice! → Great! → Fantastic! → TETRIS!* (rainbow)
- Pixel-art GAME OVER animation — independent physics bodies per block
- High-score board — top 10 persisted to `highscores.json`
- Initials entry screen on a qualifying score
- Pause overlay — music drops to 10 %, any key resumes, Q exits to menu
- Music Preview screen — audition all 10 tiers live from the menu
- Settings screen — music volume, SFX volume, display scale, ghost opacity

---

## Scoring

All scores are multiplied by `(level + 1)`.

| Event | Base points | In danger zone (above red line) |
|-------|-------------|----------------------------------|
| Single (1 line) | 40 | 80 |
| Double (2 lines) | 100 | 200 |
| Triple (3 lines) | 300 | 600 |
| Tetris (4 lines) | 1,200 | 2,400 |
| WOW bonus (perfect clear) | +5,000 | — |
| T-spin single | 800 | 1,600 |
| T-spin double | 1,200 | 2,400 |
| T-spin triple | 1,600 | 3,200 |
| T-spin mini single | 200 | 400 |
| T-spin mini double | 400 | 800 |

**Back-to-back bonus:** consecutive Tetrises or T-spins earn 1.5× on the difficult clear score.

**Combo bonus:** `50 × combo count × (level + 1)` stacked on top for each consecutive clear.
First clear in a row: no bonus. Second: 50 × (level + 1). Third: 100 × (level + 1), etc.

The danger-zone multiplier applies to the entire clear score if **any** of the cleared
rows are above the red line, making high-risk play in the top half of the board
meaningfully more rewarding.

---

## Requirements

- Python 3.10+
- pygame ≥ 2.5
- numpy ≥ 1.24

---

## Installation

```bash
pip install -r requirements.txt
python3 main.py
```

---

## Controls

### Menu
| Key | Action |
|-----|--------|
| `Space` | Start game |
| `T` | Music Preview |
| `S` | Settings |
| `H` | Leaderboard |

### Gameplay
| Key | Action |
|-----|--------|
| `←` / `→` | Move (hold for DAS auto-repeat) |
| `↑` | Rotate clockwise |
| `Ctrl+↑` or `Z` | Rotate counter-clockwise |
| `↓` | Soft drop |
| `Space` | Hard drop (instant lock) |
| `C` | Hold piece |
| `Q` / `Esc` | Pause |
| `R` | Restart |
| `M` | Toggle music mute |

### Pause
| Key | Action |
|-----|--------|
| Any key | Resume |
| `Q` | Exit to menu |

### Music Preview
| Key | Action |
|-----|--------|
| `↑` / `↓` | Select tier |
| `Enter` or `Esc` | Back to menu |

### Settings
| Key | Action |
|-----|--------|
| `↑` / `↓` | Select row |
| `←` / `→` | Adjust value |
| `Enter` or `Esc` | Back to menu |
| `M` | Toggle music mute |

---

## Settings

| Option | Range | Default | Notes |
|--------|-------|---------|-------|
| Music Volume | 0 – 100 % | 40 % | Affects both menu and game music |
| SFX Volume | 0 – 100 % | 100 % | Live preview on adjust |
| Display Scale | 1× / 1.5× / 2× / 2.5× | 1.5× | Persisted to `config.json` |
| Ghost Opacity | 0 – 100 % | 15 % | 0 % disables shadow entirely; 100 % looks like a solid tile |

---

## Project structure

```
main.py           game loop, state machine, all rendering
board.py          10×20 grid, collision detection, line clearing
piece.py          tetromino shapes, CW/CCW rotation matrices
constants.py      grid geometry, NES colour palette, scoring table, fall-speed curve
sprites.py        procedural NES-style block and ghost surfaces (cached per colour + opacity)
audio.py          procedural SFX via PCM synthesis (sawtooth + square oscillators)
music_game.py     10-tier layered chiptune engine (adaptive, sequence-driven)
music.py          standalone 32-bar chiptune composition (menu music)
particles.py      particle burst system for line clears
game_over_anim.py per-block physics animation for the GAME OVER sequence
highscore.py      JSON-backed top-10 persistence
config.py         settings persistence (scale, ghost opacity → config.json)
```

---

## License

MIT — do whatever you want with it.
